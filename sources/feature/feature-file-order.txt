Feature files have to be merged in this order.
1) optional.fea
2) punctuation.fea  # This goes before contexuals.fea to reduce the amount of characters to search.
3) contextuals.fea
4. kerning.fea

Don't merge these manually. Just merge ../features.fea (which should have the same order).