# The python implementation of font.merge() is broken so we need to use this file
# to merge the features properly.
languagesystem DFLT dflt;
languagesystem latn dflt;

include(../optional.fea);
include(../punctuation.fea);
include(../contextuals.fea);
include(../kerning.fea);
